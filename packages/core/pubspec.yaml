name: go_board_core
description: Go board recognition core — board detection, grid analysis, stone classification
version: 0.1.0

environment:
  sdk: ^3.6.0

dependencies:
  opencv_dart: ^1.3.5

dev_dependencies:
  test: ^1.25.0
