name: Build & Release APK

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.27.4'
          channel: stable

      - name: Install dependencies
        working-directory: apps/android
        run: flutter pub get

      - name: Build debug APK
        working-directory: apps/android
        run: flutter build apk --debug

      - name: Rename APK with version
        run: |
          VERSION=${{ github.ref_name }}
          cp apps/android/build/app/outputs/flutter-apk/app-debug.apk \
             apps/android/build/app/outputs/flutter-apk/go-board-recognition-${VERSION}.apk

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: apps/android/build/app/outputs/flutter-apk/go-board-recognition-${{ github.ref_name }}.apk
          prerelease: true
          name: ${{ github.ref_name }} (Closed Alpha)
          generate_release_notes: true
